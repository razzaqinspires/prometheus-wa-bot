# ==============================================================================
# Gitignore untuk Proyek Prometheus (Node.js WhatsApp Bot)
# ==============================================================================

# 1. Ketergantungan & Cache
# Direktori ini di-generate oleh 'npm install' dan ukurannya sangat besar.
node_modules/
.npm/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 2. Sesi & Kredensial (SANGAT PENTING & RAHASIA!)
# Folder ini berisi kunci login WhatsApp Anda. JANGAN PERNAH diunggah ke GitHub!
session/

# File environment variables yang berisi semua API key dan rahasia lainnya.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 3. Data, Log, & File Hasil Generate
# Semua file database yang dibuat saat bot berjalan.
data/

# Semua file log yang dihasilkan oleh Logger.js.
logs/

# File audit log yang mungkin dibuat di home directory.
# (Gunakan path absolut jika perlu, tapi pola ini lebih umum)
*.log
.bot_audit.log

# 4. File Temporer & Backup
# Folder untuk file sementara (misalnya, saat membuat stiker).
tmp/
temp/

# File backup yang di-generate oleh perintah .backup.
prometheus-backup-*.zip
*.zip
*.rar

# 5. Konfigurasi Lokal
# Jika Anda mengikuti rekomendasi di bawah untuk menggunakan .env,
# Anda bisa mengabaikan file config.js yang berisi kunci asli.
# config.js 

# 6. File Spesifik Sistem Operasi & Editor
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

.vscode/
.idea/
*.swp
*.swo